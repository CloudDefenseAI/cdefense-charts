{{- with $dot := . }}
{{- range $service := $dot.Values.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdefense.fullname" $dot }}-{{ $service.name }}
  namespace: {{ include "cdefense.namespace" $dot }}
  labels:
    {{- include "cdefense.labels" $dot | nindent 4 }}
spec:
  {{- with $service }}
    {{- omit . "name" "selector" | toYaml | nindent 2 }}
  {{- end}}
  selector:
    {{- include "cdefense.selectorLabels" $dot | nindent 4 }}
{{- end }}
{{- end }}